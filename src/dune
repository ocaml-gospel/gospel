(ocamllex
 (modules pps ulexer))

(menhir
 (modules uparser)
 (flags
  (-v --explain)))

(library
 (public_name gospel)
 (preprocess
  (pps ppx_deriving.show))
 (libraries ocaml-compiler-libs.shadow fmt ppxlib ppxlib.ast pp_loc findlib)
 (modules_without_implementation uast))
